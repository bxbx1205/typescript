function makeChai(order: {type:string; sugar:number, strong:boolean} | string) {
    console.log(order);
    
}

function serverChai(order:{type:string; sugar:number, strong:boolean} | string){
    
}